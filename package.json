{
  "name": "primer-integration-walkthrough",
  "version": "1.0.0",
  "description": "Build an end-to-end integration with Primer and start accepting payments in 3 simple steps: create a secure checkout using our Universal Checkout, tokenize the payment method and authorize a payment against the payment token.",
  "main": "server.js",
  "license": "MIT",
  "scripts": {
    "start": "nodemon ./src/server.js"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "node-fetch": "^3.0.0",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  },
  "nodemonConfig": {
    "ignore": [
      "node_modules/*"
    ],
    "watch": [
      ".env",
      "*.js",
      "*.css",
      "*.html"
    ]
  }
}
